namespace JobSearchAggregator.Domain.Enums;

/// <summary>
/// Grouping used to organize the user's static skill profile
/// (see Application layer configuration for the seeded skill list).
/// </summary>
public enum SkillCategory
{
    Language = 0,
    Backend = 1,
    Frontend = 2,
    Database = 3,
    Ai = 4,
    Cloud = 5,
    CoreConcept = 6,
    Tool = 7
}
